import './bootstrap';
import 'flowbite';
import jQuery from 'jquery';
window.$ = jQuery;
import './features/calendar.js';  // Ensure the path is correct
import './features/inputs_date_repairs.js';  // Ensure the path is correct
// FullCalendar initialization and configuration
